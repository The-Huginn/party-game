apiVersion: v1
kind: PersistentVolume
metadata:
  name: backend-pv
  labels:
    app: drinking-game
spec:
  storageClassName: backend-manual
  capacity:
    storage: 128Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /home/bifrost/k3s-volumes/backend

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-pvc
  labels:
    app: drinking-game
spec:
  storageClassName: backend-manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: drinking-game
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      volumes:
        - name: backend-pv-storage
          persistentVolumeClaim:
            claimName: backend-pvc
      containers:
      - name: backend
        image: localhost/game
        imagePullPolicy: Always

        env:
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: password
        - name: DB_HOST
          value: $(MONGO_SVC_SERVICE_HOST)
        - name: DB_PORT
          value: $(MONGO_SVC_SERVICE_PORT)
        
        resources:
          limits:
            memory: 1024Mi
            cpu: 1000m
          requests:
            memory: 256Mi
            cpu: 250m
        
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 5

        readinessProbe:
          httpGet:
            path: /ready
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 5

        ports:
        - containerPort: 5000
        
        volumeMounts:
        - mountPath: /etc/backend
          name: backend-pv-storage

---

apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: drinking-game
spec:
  selector:
    app: backend
  ports:
  - port: 5000
    targetPort: 5000